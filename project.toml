[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

# Package metadata and dependencies are defined in setup.py, which setuptools
# still reads. This file only declares the build backend so that
# `pip install -e .` uses modern PEP 660 editable installs (live edits).
